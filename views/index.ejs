<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <div>
      <% messages.forEach( message => { %>

      <p><%= message.text %> <%= message.user %>: <%= message.added %></p>
      <% }) %>
    </div>
    <div>
      <a href="catalog/new">head over to the form page</a>
    </div>
  </body>
</html>
